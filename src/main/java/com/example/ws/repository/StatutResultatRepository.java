package com.example.ws.repository;

import com.example.ws.entity.StatutResultat;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StatutResultatRepository extends JpaRepository<StatutResultat, Long> {
}